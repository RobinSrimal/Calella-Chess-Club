# Route: /[locale]/admin/users

## Intended File

```txt
packages/web/src/pages/[locale]/admin/users.astro
```

## Purpose

Admin membership and account management.

## Features

```txt
list users by membership status
approve pending membership
reject pending membership
restore rejected membership to pending/member
disable accounts
```

Rejected users remain active accounts unless disabled separately.

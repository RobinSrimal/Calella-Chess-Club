---
interface Props {
  title: string;
  summary?: string;
}

const { title, summary } = Astro.props;
---

<header class="page-intro shell-inner">
  <h1>{title}</h1>
  {summary && <p>{summary}</p>}
</header>
